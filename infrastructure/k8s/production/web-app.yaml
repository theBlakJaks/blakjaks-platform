apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
        - name: web-app
          image: us-central1-docker.pkg.dev/blakjaks-production/blakjaks-repo/web-app:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: blakjaks-config
          env:
            - name: SENTRY_DSN
              valueFrom:
                secretKeyRef: { name: blakjaks-secrets, key: SENTRY_DSN }
            - name: NEXT_PUBLIC_SENTRY_DSN
              valueFrom:
                secretKeyRef: { name: blakjaks-secrets, key: SENTRY_DSN }
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: production
spec:
  selector:
    app: web-app
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
